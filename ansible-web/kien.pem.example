Put your real AWS EC2 private key at:

ansible-web/kien.pem

The key must match the Terraform key_name value in terraform/terraform.tfvars.

Example:

cp /path/to/your-key.pem ansible-web/kien.pem
chmod 400 ansible-web/kien.pem
