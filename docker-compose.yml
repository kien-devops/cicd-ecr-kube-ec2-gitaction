version: "3"
services:
  frontend:
    container_name: cons-react
    build: ./hospital_FE
    ports:
      - 5173:8000
  backend:
    container_name: cons-dotnet
    build: ./hospital_BE/Hospital_API
    ports:
      - 5247:8080
