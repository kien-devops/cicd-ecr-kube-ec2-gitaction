# Create an internal ClusterIP Service for frontend pods.
apiVersion: v1
kind: Service
metadata:
  name: fe-service-v1
  namespace: hospital
spec:
  selector:
    app: fe-v1
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
